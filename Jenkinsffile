pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                sh './mvnw clean package'
            }
        }

        stage('Deploy') {
            steps {
                sh '''
                echo "Deployment commands go here"
                '''
            }
        }
    }
}
